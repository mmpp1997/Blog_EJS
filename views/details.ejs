<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/index.css">
    <title>My Blog Site</title>
</head>
<body>
    <div class="grid-container">
        <div>
            <%- include('partials/header.ejs') %>
        </div>
        <div class="detail">
            <% if(locals.about){%>
                <h3><%= about.title %></h3>
                <p>User <%= about.name %> asks in <%= about.topic %>:</p>
                <p><%= about.text %></p>
                <a href="/edit/<%=about.id%>">Edit Post</a> 
                <a href="/delete/<%=about.id%>">Delete Post</a>      
            <%}else{%>
                <p class="empty">This post doesn't exist</p>
            <%}%>
        </div>
        <div>
            <%- include('partials/footer.ejs') %>
        </div>
    </div>
</body>
</html>