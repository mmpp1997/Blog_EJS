<head>
    <link rel="stylesheet" href="/styles/header.css">
</head>
<header>
    <a class="title" href="/">My Blog Site</a>
    <%if(locals.topics){%>
        <form class="header-form" action="/filter" method="POST">
            <select name="topic"  onchange="this.form.submit()">
                <option>All Posts</option>
                <%for(i=0;i<topics.length;i++){%>
                <option><%=topics[i].name%></option>
                <%}%>
                <option hidden selected="selected" disabled>Filter</option>
            </select>
        </form>
    <%}%>
    <a class="new-btn" style="text-align:center" href="/form">New post</a>
</header>